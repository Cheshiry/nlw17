const start = () => {
    let count = 0;
    while (true){
        const opcao await = "cadastrar";
        
        switch (opcao) {
            case "sair":
                console.log("Vamos cadastar")
                break;
        
            case "listar":
				console.log("vamos listar");
                break;

            case "sair":
				return;
                
        }
    }
}
//npm install inquirer

start();